;;; This file is used for interactively testing window-stool
;;; Should not be evaluated

(defun random-test-function ()
  "This is a function."
  (let ((one 1)
        (two 2))
    (progn
      (when 1
        (+ one two)
        )
      )
    )
  )

;;; SPACER






























;;; SPACER so that this requires scrolling to get to

(defun random-test-function2 ()
  "This is another function."
  (let ((one 1)
        (two 2))
    (progn
      (when 1
        (+ one two)
        )
      )
    )
  )

;;; SPACER




























;;; SPACER
